sonar.projectKey=javintrivedi_campus-compass-main
sonar.organization=javintrivedi

# This is the name and version displayed in the SonarCloud UI.
sonar.projectName=campus-compass-main
sonar.projectVersion=1.0

# Only analyze the backend server code (not frontend, not terraform)
sonar.sources=backend/server.js

# Quality Gate configuration
sonar.qualitygate.wait=false

# Encoding of the source code.
sonar.sourceEncoding=UTF-8

# Coverage reporting (path relative to project root)
sonar.javascript.lcov.reportPaths=backend/coverage/lcov.info
